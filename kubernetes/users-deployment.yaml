apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: vladtraum/kub-network-app-v3-users:latest
          env:
            - name: AUTH_DOMAIN
              value: "10.103.188.181" # auth-service ClusterIP